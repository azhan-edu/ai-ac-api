[
  {
    "task": "Extract the AWS region from an S3 bucket URL like 's3://my-bucket.us-west-2.amazonaws.com/path/to/file'",
    "format": "regex"
  },
  {
    "task": "Parse an AWS CloudWatch log entry and extract the timestamp, log level, and message",
    "format": "python"
  },
  {
    "task": "Create a valid IAM policy JSON object that allows read-only access to a specific S3 bucket named 'company-data'",
    "format": "json"
  }
]